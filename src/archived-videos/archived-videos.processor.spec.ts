import { ArchivedVideosProcessor } from './archived-videos.processor';

describe('ArchivedVideosProcessor', () => {
  it('should be defined', () => {
    expect(new ArchivedVideosProcessor()).toBeDefined();
  });
});
