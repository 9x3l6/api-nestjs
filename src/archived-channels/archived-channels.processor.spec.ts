import { ArchivedChannelsProcessor } from './archived-channels.processor';

describe('ArchivedChannelsProcessor', () => {
  it('should be defined', () => {
    expect(new ArchivedChannelsProcessor()).toBeDefined();
  });
});
